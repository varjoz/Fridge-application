-------Frontend
Kirajzolja a
    - hutot
    - kategoriakat
AJAX keres 
    return kategorialista


----------Backend
// - oda kell majd a $request ellenorzese, nagyjabol olyan formatumban mint az akasztofaban csinaltad.
// A $fridge-el fogsz tudni dolgozni
//index.php
$request = new Request();
$fridge = new Fridge('termekek.txt','kategoriak.txt');


// - Fajlkezeleshez fuggvenyek
File class 
	public $filename;

    __construct($filename) 
        $this->filename = $filename;

	fuggvenyek: 
		- read_from_file($filename) - fajlbol olvas     
            return $returnData;
		- save_to_file($data) - fajlba ir 
            file_put_contents($this->filename, serialize($data));


// - Vizsgalathoz fuggvenyek
Request class ()
    public $get;


    __construct()
        $this->get = $_GET;

	fuggvenyek: 
		- has('valami') - vizsgalja, h letezik-e 'valami' az URL-ben
		- get('data') - visszater az URL "data" kulcs ertekevel 

//2 lehetoseged van.
// (a) a kategoria meg a termek csak egy objektum, van neve, a termeknek pluszban kategoriaja es ennyi.
// A szurest, listazast ez esetben a fridge csinalja
// (b) ezek az osztalyok azokat a muveleteket, amiket veluk vegzunk, tudjak onmaguktol. Mit ertek ezalatt?

//kategoria megrajzolasa, elhelyezese
Kategoria class ()
    category_array[];
    public $category_name;

    __construct($name)
            $this->category_name = $name;

    fuggvenyek:
        - print_categories();  // - osszes kategoria listazasa


//uniqid-t  temahoz: https://www.php.net/manual/en/function.uniqid.php
// termek adatai es a rajta elvegezheto muveletek 
Termek class 
    products_array[];

    __construct(product_name, product_id, category, exp_date, product_note)
            uniqid(string $prefix = "", bool $more_entropy = false): string - uniqid letrehozasa

    fuggvenyek:
        - print_products(); - osszes termek listazasa
        - add_product(product_name, category, exp_date, product_note); - termek hozzaadasa
        - delete_product(product_id) - termek eltavolitasa
        - filter_product('szuresi_feltetel');  // - szures valami menten
        // - products_array tomb lekerese (szuresi feltetellel vagy anelkul)
        // - edit_product(product_id);


// Ha mar fridge, a Fridge class egesz jo az szerintem tudja majd a dolgat
// "osszefogja" valamint kezeli az uzleti logikat is, ha lehet. (szinten voting app asszem)
Fridge class  () 
    require_once() - osztalyok behuzasa
    public $product_list_file;
    public $category_list_file;
    public $product_list[];
    public $category_list[];

    __construct(file_nama1, file_nama2)
        $this-$product_list_file = new File($file_name1);
        $this->category_list_file = new File($file_name2);
        $this->load();

    //uzleti logika - voting app//
        
    fuggvenyek:
        - load() - termeklista_frissitese
            $this->product_list = $this->product_list_file->read();
            $this->category_list = $this->category_list_file->read();
        
       
